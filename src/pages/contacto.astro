---
import Layout from "../layouts/Layout.astro";
import Newsletter from "../components/Newsletter.astro";
---

<Layout title="Contacto - TEA Friendly ®">
  <div class="bg-gray-50 min-h-screen py-16 px-4 sm:px-6 lg:px-8">
    <div class="max-w-xl mx-auto">
      <div class="text-center">
        <h2 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
          Contáctanos
        </h2>
        <p class="mt-4 text-lg text-gray-500">
          ¿Tienes dudas sobre la certificación? ¿Quieres colaborar? Escríbenos.
        </p>
        <div class="mt-6">
          <a href="mailto:hola@teafriendly.es" class="text-2xl font-bold text-tea-blue hover:underline">
            hola@teafriendly.es
          </a>
        </div>
      </div>
      <div class="mt-12">
        <form
          action="https://formspree.io/f/xaqnqwwj"
          method="POST"
          class="grid grid-cols-1 gap-y-6 sm:grid-cols-2 sm:gap-x-8 bg-white p-8 rounded-2xl shadow-lg border border-gray-100"
        >
          <div class="sm:col-span-2">
            <label for="name" class="block text-sm font-bold text-gray-700 mb-1"
              >Nombre</label
            >
            <div class="mt-1">
              <input
                type="text"
                name="name"
                id="name"
                autocomplete="name"
                class="py-3 px-4 block w-full shadow-sm focus:ring-tea-blue focus:border-tea-blue border-gray-300 rounded-xl border transition-all"
                required
                placeholder="Tu nombre"
              />
            </div>
          </div>
          <div class="sm:col-span-2">
            <label
              for="email"
              class="block text-sm font-bold text-gray-700 mb-1">Email</label
            >
            <div class="mt-1">
              <input
                type="email"
                name="email"
                id="email"
                autocomplete="email"
                class="py-3 px-4 block w-full shadow-sm focus:ring-tea-blue focus:border-tea-blue border-gray-300 rounded-xl border transition-all"
                required
                placeholder="tu@email.com"
              />
            </div>
          </div>
          <div class="sm:col-span-2">
            <label
              for="message"
              class="block text-sm font-bold text-gray-700 mb-1">Mensaje</label
            >
            <div class="mt-1">
              <textarea
                id="message"
                name="message"
                rows="4"
                class="py-3 px-4 block w-full shadow-sm focus:ring-tea-blue focus:border-tea-blue border-gray-300 rounded-xl border transition-all"
                required
                placeholder="¿En qué podemos ayudarte?"></textarea>
            </div>
          </div>
          <div class="sm:col-span-2">
            <button
              type="submit"
              class="w-full inline-flex items-center justify-center px-6 py-4 border border-transparent rounded-full shadow-lg text-base font-bold text-white bg-tea-blue hover:bg-blue-600 hover:shadow-glow-blue transition-all duration-300 transform hover:-translate-y-1"
            >
              Enviar Mensaje
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <Newsletter />
</Layout>
